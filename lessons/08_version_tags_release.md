# 第8课：版本标签与发布
目标：用标签记录版本并推送到远程，准备发布说明。

1. 创建语义化版本标签
   - git tag -a v0.1.0 -m "初版：下落、旋转、消行与计分"
2. 推送标签
   - git push --tags
3. 在 GitHub 创建 Release（可选）
   - 进入仓库的 Releases 页面，选择 v0.1.0，填写发布说明
4. 版本迭代建议
   - 新功能或修复分别使用 minor/patch 升级（如 v0.2.0、v0.1.1）
